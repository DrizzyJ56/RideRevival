version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm run build
    build:
      commands:
        - npm run preview
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
